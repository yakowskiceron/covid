<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.programacion.covid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconomain"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconomain"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RespuestaActivity"
            android:parentActivityName=".view.ContainerActivity"></activity>
        <activity
            android:name=".Noticias"
            android:parentActivityName=".view.ContainerActivity"></activity>
        <activity
            android:name=".Correo"
            android:parentActivityName=".Perfil" />
        <activity
            android:name=".Perfil"
            android:parentActivityName=".view.ContainerActivity" />
        <activity android:name=".view.ContainerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>